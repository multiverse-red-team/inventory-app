///////////////////////////////////////////////////////////////////////////////////////////
// This section tests if an item is defined when it is sent into the Inventory database //
/////////////////////////////////////////////////////////////////////////////////////////

test('That an item name is defined when added to inventory', () => {
    const itemname = null;
    expect(itemname[n]).toBeDefined();
 });

 test('That an item description is defined when added to inventory', () => {
    const itemdescription = null;
    expect(itemdescription[n]).toBeDefined();
 });

 test('That an item category is defined when added to inventory', () => {
    const itemcategory = null;
    expect(itemcategory[n]).toBeDefined();
 });

 test('That an item image is defined when added to inventory', () => {
    const itemimage = null;
    expect(itemimage[n]).toBeDefined();
 });


///////////////////////////////////////////////////////////////////////////////////////////
// This section tests......                                                             //
/////////////////////////////////////////////////////////////////////////////////////////